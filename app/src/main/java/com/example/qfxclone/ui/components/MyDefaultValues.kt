package com.example.qfxclone.ui.components

import androidx.compose.foundation.layout.size
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

object MyDefaultValues {
    val defaultSpace = Modifier.size(0.dp, 24.dp)
    val defaultBookingSpace = Modifier.size(0.dp, 12.dp)
    val defaultBgColor = Color(63, 63, 63, 255)
    //    val defaultBgColor = Color(118, 217, 121, 255)
    val defaultButtonColor = Color(80, 190, 215, 255)
}